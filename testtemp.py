import os
import django
import config.development.settings

from config.development.settings import BASE_DIR
from config.development.settings import PROJECT_ROOT
from config.development.settings import INSTALLED_APPS

print(BASE_DIR)
print(PROJECT_ROOT)
print(INSTALLED_APPS)

